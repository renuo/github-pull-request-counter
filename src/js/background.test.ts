import serviceWorker from './background';
// import { globalMock } from '../../__test__/mocks/github-api-mock-data';

describe('background', () => {
  beforeEach(() => {
  })

  it('s', async() => {
    // await serviceWorker();
  });
});
