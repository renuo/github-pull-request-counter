version: "v1.0"
name: github-pull-request-counter
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
auto_cancel:
  running:
    when: "true"
blocks:
  - name: Test
    task:
      secrets:
        - name: github-pull-request-counter
      prologue:
        commands:
          - checkout --use-cache
          - cache restore
          - nvm install
          - npm install
          - cache store
      jobs:
        - name: tests
          commands:
            - bin/check
promotions:
  - name: deploy
    pipeline_file: deploy.yml
    auto_promote:
      when: "branch = 'main'"
